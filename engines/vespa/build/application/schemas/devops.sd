schema devops {
    document devops {
        field title type string {
            indexing: summary | index | attribute
            match: text
        }

        field body type string {
            indexing: summary | index | attribute
            match: text
        }
    }

    fieldset default {
        fields: title, body
    }

    rank-profile default {
        first-phase {
            expression: bm25(title, body)
        }
    }
}